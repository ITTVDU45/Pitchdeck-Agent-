# Siehe apps/web/.env – Next.js lädt Umgebungsvariablen aus apps/web/.env
#
# PostgreSQL (lokal mit docker compose):
# DATABASE_URL="postgresql://pitchdeck:pitchdeck@localhost:5432/pitchdeck"
#
# Vercel: dieselbe DATABASE_URL wie bei Neon/Supabase eintragen (kein file:).

OPENAI_API_KEY=
OPENAI_MODEL=gpt-4o-mini
