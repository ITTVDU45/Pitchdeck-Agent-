# Optional: OpenAI für Konzept-Generierung (JSON). Ohne Key: deterministischer Fallback.
OPENAI_API_KEY=
OPENAI_MODEL=gpt-4o-mini

# Später: ElevenLabs, Datenbank, Object Storage (siehe docs/technische-codex-roadmap.md)
